name: Frontend Deploy - Staging

on:
  push:
    branches:
      - staging
  workflow_dispatch:
    inputs:
      environment:
        description: "Confirm deployment target"
        required: true
        type: choice
        options:
          - staging

permissions:
  contents: read

env:
  NODE_VERSION: "20"
  PNPM_VERSION: "10"

concurrency:
  group: frontend-deploy-staging
  cancel-in-progress: true

jobs:
  deploy-staging:
    name: Deploy to Staging Server
    runs-on: ubuntu-latest
    timeout-minutes: 25
    environment:
      name: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Verify build
        run: pnpm build

      - name: Create deployment archive
        run: |
          tar \
            --exclude='.git' \
            --exclude='node_modules' \
            --exclude='.next' \
            --exclude='.github' \
            -czf /tmp/energy-iq-fe-staging.tar.gz .

      - name: Setup SSH agent
        uses: webfactory/ssh-agent@v0.9.0
        with:
          ssh-private-key: ${{ secrets.SSH_KEY }}

      - name: Add SSH known_hosts
        env:
          SSH_HOST: ${{ secrets.SERVER_IP }}
        run: |
          mkdir -p ~/.ssh
          ssh-keyscan -H "$SSH_HOST" >> ~/.ssh/known_hosts

      - name: Upload archive
        env:
          SSH_HOST: ${{ secrets.SERVER_IP }}
          SSH_USER: ${{ secrets.SERVER_USER }}
        run: |
          scp /tmp/energy-iq-fe-staging.tar.gz \
            "$SSH_USER@$SSH_HOST:/tmp/energy-iq-fe-staging.tar.gz"

      - name: Write environment file on server
        env:
          SSH_HOST: ${{ secrets.SERVER_IP }}
          SSH_USER: ${{ secrets.SERVER_USER }}
          APP_ENV_FILE: ${{ secrets.APP_ENV_FILE }}
        run: |
          printf '%s' "${APP_ENV_FILE}" | \
            ssh "$SSH_USER@$SSH_HOST" \
            "umask 077 && cat > /tmp/fe-staging.env"

      - name: Run remote deployment
        env:
          SSH_HOST: ${{ secrets.SERVER_IP }}
          SSH_USER: ${{ secrets.SERVER_USER }}
        run: |
          ssh "$SSH_USER@$SSH_HOST" \
            "APP_DIR='/home/${{ secrets.SERVER_USER }}/frontend/staging' \
             SERVICE_NAME='fe-dev' \
             ENV_FILE_PATH='/tmp/fe-staging.env' \
             bash -s" < ./scripts/deploy-ubuntu-frontend.sh

      - name: Health check
        env:
          HEALTHCHECK_URL: ${{ secrets.HEALTHCHECK_URL }}
        run: |
          sleep 10
          curl --fail --show-error --silent "$HEALTHCHECK_URL" > /dev/null

      - name: Notify success
        run: echo "Staging deployment complete - ${{ secrets.HEALTHCHECK_URL }}"
